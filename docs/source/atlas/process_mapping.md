% This page is hand-written Markdown: edit it directly. Tables pulled in with an
% include directive come from docs/source/_generated/ and are regenerated by
% src/generate_docs.py; edit the data, not the fragment.

# Process mapping

Every correspondence between the cognitive processes in the Catalog and the concepts in
the Cognitive Atlas, in both directions: a summary of each direction by match type, then
the full row tables. The [Atlas mapping method](../methods/atlas_mapping_method.md) page
explains what the match types mean and how each row was decided, and
[The Catalog vs the Atlas](the_catalog_vs_the_atlas.md) says what the correspondence
adds up to. The source of record is `data/mappings/`, not this page.

The same method was applied to this second axis as to the tasks. This mapping was built
from nothing: the Catalog's processes carry no Atlas concept identifier of any kind, so
there was no prior linkage to correct, only to construct.

## Catalog to Atlas

Processes in the Catalog by the match type of their primary Atlas concept:

```{include} ../_generated/mapping_process_forward.md
```

One row per process in the Catalog. `Atlas tasks` counts the Atlas task entries that
assert the matched concept, which is a rough measure of how much use the Atlas makes of
it.

```{include} ../_generated/atlas_process_forward.md
```

## Atlas to Catalog, matched concepts

```{include} ../_generated/mapping_process_matched_line.md
```

Atlas concepts by match type:

```{include} ../_generated/mapping_process_reverse.md
```

One row per Atlas concept with a counterpart in the Catalog.

```{include} ../_generated/atlas_process_matched.md
```

## Atlas to Catalog, concepts with no counterpart

Roughly half of the Atlas concept layer is asserted by no Atlas task either, so a concept
appearing here is not evidence that it matters to anyone.

```{include} ../_generated/atlas_process_unmatched.md
```

## How the verdicts were decided

The `related` verdict does most of the interesting work here. It marks the cases where
the Atlas carries only a broader parent: `Affective priming` against `priming`,
`Social perception` against `perception`, `Fine motor control` against `motor control`.
Recording those as `close` would overstate what the Atlas offers.

The `none` verdicts fall into two groups. Some are constructs the Atlas never
registered as concepts even though it registers them as tasks, such as `Antisaccade` and
`Reversal learning`. Others are vocabulary that post-dates the Atlas's curation, notably
`Model-based learning`, `Model-free learning` and the general form of reward prediction
error, for which the Atlas has only `monetary reward prediction error`.

Definitions, not names, decided these. The Atlas concept named `acoustic processing`
looks like an exact match for the process of the same name, but its definition describes
"signals propagated undersea, in the atmosphere" -- sonar, not audition -- so it is
recorded as `close` with that noted. `Perspective taking` matched `worldview` by name
alone and was reassigned to `theory of mind`.
